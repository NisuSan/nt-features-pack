import { readFileSync } from 'node:fs'
import { defineNuxtModule } from '@nuxt/kit'
import { defaultColorShema, tailwindFileContent, injectColorsToTailwind, createGenerableComposables, optionProviderPluginGenerator } from './theme.templates.ts'
import { createFile, resolve, resolveBuild } from '../../utils/index.ts'

export interface ModuleOptions {
  disable: boolean,
  target: 'naive-ui' | 'css',
  location: 'internal' | 'external',
  scssMixins: [string[], 'apppend' | 'replace'],
  themeCss: [string[], 'apppend' | 'replace'],
  themeCode: `${string} => ${string}`
}

export default defineNuxtModule<ModuleOptions>({
  meta: {
    name: 'themeGenerator',
    configKey: 'themeGenerator',
  },
  setup(_options, _nuxt) {
    try {
      const isInternal = _options.location === 'internal'
      // optionProviderPluginGenerator(_options.themeCode)

      // const themeDir = isInternal ? 'autogenerated/client/theme' : '../theme'
      // const rootDir = resolveBuild(themeDir, 'root')

      // createFile(resolveBuild(rootDir, 'theme.colors.ts'), `export default ${JSON.stringify(defaultColorShema)}`)
      injectColorsToTailwind(isInternal)

      // const mixins = (_options.scssMixins![1] === 'apppend' ? readFileSync(resolve('../theme_generator/source/mixins.scss'), 'utf-8').toString() + '\n\n' : '') + _options.scssMixins![0].map(x => `/*content from <${x}>*/\n${readFileSync(x, 'utf-8').toString()}`).join('\n\n')
      // createFile(resolveBuild(rootDir, `theme.mixins.scss`), mixins)

      // const css = (_options.themeCss![1] === 'apppend' ? (_options.target === 'naive-ui' ? readFileSync(resolve('../theme_generator/source/naiveUi.scss'), 'utf-8').toString() : '') + '\n\n' : '') + _options.themeCss![0].map(x => `/*content from <${x}>*/\n${readFileSync(x, 'utf-8').toString()}`).join('\n\n')
      // createFile(resolveBuild(rootDir, `theme.styles.scss`), css)

      // const content = tailwindFileContent + ['mixins', 'styles'].map(x => `\n@include "${rootDir}/theme.${x}.scss";`).join('')
      // createFile(resolve('../../tailwindcss/tailwind.css'), content)

      // createGenerableComposables()
    } catch (error) {
      console.error(error)
    }
  },
})

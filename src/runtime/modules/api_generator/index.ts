import { resolve } from 'node:path'
import { defineNuxtModule, addImports } from '@nuxt/kit'
import { composableApiTemplate } from './api.templates.ts'
import { createFile } from '../../utils/index.ts'

export default defineNuxtModule({
  meta: {
    name: 'apiComposableGenerator',
    configKey: 'apiComposableGenerator',
  },
  setup(_options, _nuxt) {
    try {
      const rootDir = resolve(_nuxt.options.buildDir, 'autogenerated')
      const file = resolve(rootDir, 'client/composables/index.ts')

      createFile(file, composableApiTemplate())
      addImports({ name: 'api', from: file })
    } catch (error) {
      console.error(error)
    }
  },
})
